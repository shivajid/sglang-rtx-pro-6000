- This is a single GPU inference benchmark for datalab-to/chandra-ocr-2 model. 
- The model is Qwen3.5 based 9b model, which can fit on single GPU.
- The base benchmarks are good.

This has not been run on a GKE instance but the scripts are of GCE instance.
