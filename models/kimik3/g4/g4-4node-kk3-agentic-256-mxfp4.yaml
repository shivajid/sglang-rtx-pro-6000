apiVersion: v1
kind: PersistentVolume
metadata:
  name: kimik3-hdml-ro-pv
spec:
  storageClassName: ""
  capacity:
    storage: 2000Gi
  accessModes:
    - ReadOnlyMany
  claimRef:
    namespace: default
    name: kimik3-hdml-pvc
  csi:
    driver: pd.csi.storage.gke.io
    volumeHandle: projects/northam-ce-mlai-tpu/zones/us-central1-b/disks/kimik3-hyperdisk-ml
    fsType: ext4
    readOnly: true
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: kimik3-hdml-pvc
  namespace: default
spec:
  storageClassName: ""
  accessModes:
    - ReadOnlyMany
  resources:
    requests:
      storage: 2000Gi
  volumeName: kimik3-hdml-ro-pv
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: sglang-sa
  namespace: default
  annotations:
    iam.gke.io/gcp-service-account: workload-identity-gcp-sa@northam-ce-mlai-tpu.iam.gserviceaccount.com
---
# Headless Service for distributed multi-node master discovery (Pod Index 0 / Node Rank 0)
apiVersion: v1
kind: Service
metadata:
  name: sglang-kimi-k3-master
  namespace: default
  labels:
    app: sglang-kimi-k3-g4
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app: sglang-kimi-k3-g4
    apps.kubernetes.io/pod-index: "0"
  ports:
  - name: dist-port
    port: 20000
    targetPort: 20000
---
# Serving Service to route inference traffic to the Master Pod
apiVersion: v1
kind: Service
metadata:
  name: sglang-kimi-k3-serving
  namespace: default
  labels:
    app: sglang-kimi-k3-g4
spec:
  type: NodePort
  selector:
    app: sglang-kimi-k3-g4
    apps.kubernetes.io/pod-index: "0"
  ports:
  - name: http-inference
    port: 30000
    targetPort: 30000
---
# 4-Node Distributed StatefulSet with Pipeline Parallelism (PP=4, TP=8) on Hyperdisk ML
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: sglang-kimi-k3-g4
  namespace: default
  labels:
    app: sglang-kimi-k3-g4
spec:
  podManagementPolicy: Parallel
  replicas: 4
  serviceName: "sglang-kimi-k3-master"
  selector:
    matchLabels:
      app: sglang-kimi-k3-g4
  template:
    metadata:
      labels:
        app: sglang-kimi-k3-g4
        inference.networking.k8s.io/engine-type: sglang
      annotations:
        networking.gke.io/default-interface: "ens4"
    spec:
      serviceAccountName: sglang-sa
      hostNetwork: true
      hostIPC: true
      dnsPolicy: ClusterFirstWithHostNet
      nodeSelector:
        cloud.google.com/gke-nodepool: chavoshi-v10-pool
      tolerations:
      - key: "nvidia.com/gpu"
        operator: "Exists"
        effect: "NoSchedule"
      - key: "node.kubernetes.io/disk-pressure"
        operator: "Exists"
        effect: "NoSchedule"
      containers:
      - name: sglang-container
        image: gcr.io/northam-ce-mlai-tpu/sglang-k3-mxfp8:latest
        imagePullPolicy: IfNotPresent
        securityContext:
          privileged: true
          capabilities:
            add: ["IPC_LOCK", "SYS_PTRACE"]
        command:
        - /bin/bash
        - -c
        - |
          set -e
          mkdir -p /dev/shm/hf_cache /dev/shm/tmp || true
          echo "=== STARTING KIMI-K3 SGLANG SERVE ON G4 WITH HYPERDISK ML (NODE RANK: ${POD_INDEX}) ==="
          MY_IP=$(hostname -I | awk '{print $1}')
          echo "Host IP: ${MY_IP}"
          echo "Node Rank: ${POD_INDEX} / Total Nodes: 4"
          echo "Master Address: sglang-kimi-k3-master:20000"

          # NOTE: SM120 kernel-guard fixes (is_sm100_supported guards) are baked
          # into the sglang-k3-mxfp8 image; no runtime hot-patch is needed.

          # --- SGLang Server Execution ---
          # MoE: flashinfer_mxfp4 routes SM120 -> cutlass_sm120 (fused-MoE path)
          # CUDA-graph decode: 'breakable' backend with batch sizes up to 64
          # HiCache: spills KV prefix cache to host RAM for multi-turn agent sessions
          # Triton attention: 16 KV splits for long-context decode
          sglang serve \
            --model-path /data/model \
            --served-model-name moonshotai/Kimi-K3 \
            --tp-size 8 \
            --pp-size 4 \
            --nnodes 4 \
            --node-rank ${POD_INDEX} \
            --dist-init-addr sglang-kimi-k3-master:20000 \
            --moe-runner-backend flashinfer_mxfp4 \
            --attention-backend triton \
            --watchdog-timeout 3600 \
            --skip-server-warmup \
            --mem-fraction-static 0.86 \
            --kv-cache-dtype fp8_e4m3 \
            --max-mamba-cache-size 256 \
            --context-length 262144 \
            --enable-hierarchical-cache \
            --hicache-ratio 1.0 \
            --hicache-write-policy write_through \
            --hicache-io-backend direct \
            --hicache-mem-layout page_first \
            --triton-attention-num-kv-splits 16 \
            --reasoning-parser kimi_k3 \
            --tool-call-parser kimi_k3 \
            --host 0.0.0.0 \
            --port 30000 \
            --trust-remote-code \
            --enable-metrics || true

          # Keep container alive if sglang serve exits for post-mortem debugging
          echo "sglang serve process finished or exited. Keeping container alive for debugging..."
          sleep infinity
        env:
        - name: POD_INDEX
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.labels['apps.kubernetes.io/pod-index']
        - name: HF_HOME
          value: /dev/shm/hf_cache
        - name: TMPDIR
          value: /dev/shm/tmp
        # NCCL / Inter-node Communication for G4 Titanium Networking & PCIe
        - name: NCCL_IB_DISABLE
          value: "1"
        - name: NCCL_SOCKET_IFNAME
          value: "eth0, eth1"
        - name: NCCL_P2P_LEVEL
          value: "SYS"
        - name: NCCL_SOCKET_NTHREADS
          value: "8"
        - name: NCCL_NSOCKS_PERTHREAD
          value: "8"
        - name: NCCL_MIN_NCHANNELS
          value: "8"
        - name: NCCL_DEBUG
          value: "INFO"
        - name: NCCL_SOCKET_RCVBUF
          value: "8388608"
        - name: NCCL_SOCKET_SNDBUF
          value: "8388608"
        - name: NCCL_BUFFSIZE
          value: "16777216"
        - name: NCCL_PROTO
          value: "Simple"
        - name: OMP_NUM_THREADS
          value: "24"
        - name: SGLANG_SET_CPU_AFFINITY
          value: "0"
        - name: SAFETENSORS_FAST_GPU
          value: "0"
        - name: PYTORCH_CUDA_ALLOC_CONF
          value: "expandable_segments:True"
        - name: SGLANG_REQUEST_STATE_WAIT_TIMEOUT
          value: "18000"
        - name: SGLANG_SITU_MXFP8_ACT
          value: "1"
        - name: HF_TOKEN
          valueFrom:
            secretKeyRef:
              name: hf-secret
              key: HF_TOKEN
              optional: true
        resources:
          limits:
            nvidia.com/gpu: "8"
          requests:
            nvidia.com/gpu: "8"
        volumeMounts:
        - name: hyperdisk-model-volume
          mountPath: /data/model
          readOnly: true
        - name: dshm
          mountPath: /dev/shm
      volumes:
      - name: hyperdisk-model-volume
        persistentVolumeClaim:
          claimName: kimik3-hdml-pvc
          readOnly: true
      - name: dshm
        emptyDir:
          medium: Memory
          sizeLimit: 64Gi

